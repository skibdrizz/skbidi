local Players = game:GetService("Players")
local player = Players.LocalPlayer
local character = player.Character or player.CharacterAdded:Wait()
local hrp = character:WaitForChild("HumanoidRootPart")
local humanoid = character:WaitForChild("Humanoid")

local eggName = "Egg"
local SCAN_INTERVAL = 0.5
local REACHED_DISTANCE = 5
local TIMEOUT = 5
local USE_TOUCH = true
local MAX_FAILED_ATTEMPTS = 3
local RESET_AFTER_PICK = 5 -- số lần reset sau khi lấy egg

local triedEggs = {}
local failedAttempts = 0

-- Tìm egg gần nhất chưa thử
local function findNearestEgg()
	local closestEgg, minDist = nil, math.huge

	for _, obj in workspace:GetDescendants() do
		if obj:IsA("MeshPart") and obj.Name == eggName and obj:IsDescendantOf(workspace) then
			if not triedEggs[obj] then
				local dist = (hrp.Position - obj.Position).Magnitude
				if dist < minDist then
					closestEgg = obj
					minDist = dist
				end
			end
		end
	end

	return closestEgg
end

-- Chạm để nhặt (nếu cần)
local function touchEgg(egg)
	if not USE_TOUCH then return end

	pcall(function()
		firetouchinterest(hrp, egg, 0)
		task.wait(0.1)
		firetouchinterest(hrp, egg, 1)
	end)
end

-- Reset nhân vật
local function resetCharacter()
	local humanoid = character:FindFirstChildOfClass("Humanoid")
	if humanoid then
		humanoid.Health = 0
	end
end

-- Đợi nhân vật hồi sinh
local function waitForRespawn()
	character = player.CharacterAdded:Wait()
	hrp = character:WaitForChild("HumanoidRootPart")
	humanoid = character:WaitForChild("Humanoid")
end

-- Teleport đến egg
local function teleportTo(pos)
	hrp.CFrame = CFrame.new(pos + Vector3.new(0, 5, 0))
end

-- Main loop
while true do
	local egg = findNearestEgg()

	if egg then
		triedEggs[egg] = true
		print("🎯 Tìm thấy egg, teleport tới...")
		teleportTo(egg.Position)

		-- Đi bộ đến egg để chạm
		humanoid:MoveTo(egg.Position)
		touchEgg(egg)

		local timer = 0
		repeat
			task.wait(0.1)
			timer += 0.1
			if USE_TOUCH then touchEgg(egg) end
		until not egg:IsDescendantOf(workspace) or timer >= TIMEOUT or (hrp.Position - egg.Position).Magnitude <= REACHED_DISTANCE

		if egg:IsDescendantOf(workspace) then
			warn("⚠️ Egg vẫn chưa được lấy sau " .. TIMEOUT .. "s, bỏ qua.")
			failedAttempts += 1
		else
			print("✅ Đã lấy egg. Reset " .. RESET_AFTER_PICK .. " lần...")
			failedAttempts = 0

			for i = 1, RESET_AFTER_PICK do
				resetCharacter()
				waitForRespawn()
			end

			triedEggs = {} -- reset danh sách egg đã thử sau khi lấy thành công
		end

		if failedAttempts >= MAX_FAILED_ATTEMPTS then
			warn("❌ Kẹt quá 3 lần, reset nhân vật.")
			resetCharacter()
			waitForRespawn()
			triedEggs = {}
			failedAttempts = 0
		end
	else
		task.wait(SCAN_INTERVAL)
	end
end
